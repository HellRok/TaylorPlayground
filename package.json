{
  "scripts": {
    "build-index": "esbuild app/javascripts/index.js --bundle --outfile=public/index.js",
    "build-shell": "esbuild app/javascripts/shell.js --bundle --outfile=public/playground/shell.js",
    "build-css": "esbuild app/stylesheets/index.css --bundle --outfile=public/index.css",
    "build": "npm run build-index && npm run build-shell && npm run build-css"
  },
  "devDependencies": {
    "esbuild": "^0.25.9"
  }
}
