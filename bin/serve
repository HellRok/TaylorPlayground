#!/usr/bin/env bash
set -euo pipefail

clean() {
  rm -rf exports/*
}

setup-folders() {
  mkdir -p dist/playground
}

build() {
  ./bin/build
  npm run build
}

serve() {
  pushd dist
  ruby -run -e httpd . --port ${PORT:-3001}
  popd
}

clean
setup-folders
build
serve
